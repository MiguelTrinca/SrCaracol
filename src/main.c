/*
  file: main.cpp
*/

# include <stdio.h>

void parse_inputs_or_exit() {
	int nodes_num;
	int edges_num;

	sscanf(str, "%d", &nodes_num);
	sscanf(str, "%d", &edges_num);
}

int main(int argc, char const *argv[]) {

	return 0;
}